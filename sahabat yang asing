<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>Sahabat yang Asing</title>
  <style>
    html, body {
      margin: 0;
      padding: 0;
      height: 100%;
      overflow: hidden;
      font-family: 'Segoe UI', sans-serif;
      color: white;
    }

    video.bg-video {
      position: fixed;
      top: 50%;
      left: 50%;
      min-width: 100%;
      min-height: 100%;
      width: auto;
      height: auto;
      z-index: -1;
      transform: translate(-50%, -50%);
      object-fit: cover;
    }

    .container {
      position: relative;
      z-index: 1;
      height: 100%;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 20px;
      backdrop-filter: blur(4px);
    }

    h1 {
      font-size: 48px;
      margin-bottom: 20px;
      color: #f0eaff;
      text-shadow: 1px 1px 5px rgba(0, 0, 0, 0.6);
    }

    p {
      font-size: 18px;
      margin-bottom: 25px;
      max-width: 700px;
      line-height: 1.6;
      text-shadow: 1px 1px 4px rgba(0, 0, 0, 0.6);
    }

    .buttons {
      display: flex;
      gap: 15px;
      flex-wrap: wrap;
      justify-content: center;
    }

    .buttons button {
      background-color: rgba(255, 255, 255, 0.15);
      border: 1px solid #ccc;
      color: #fff;
      padding: 12px 20px;
      border-radius: 10px;
      cursor: pointer;
      font-size: 16px;
      backdrop-filter: blur(4px);
      transition: 0.3s;
    }

    .buttons button:hover {
      background-color: rgba(255, 255, 255, 0.25);
      transform: scale(1.05);
    }

    #story, #memories {
      display: none;
      margin-top: 20px;
      animation: fadeIn 1s ease-in-out;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .credit-big {
      position: absolute;
      bottom: 20px;
      left: 50%;
      transform: translateX(-50%);
      font-size: 20px;
      background-color: rgba(0, 0, 0, 0.5);
      padding: 10px 20px;
      border-radius: 12px;
      font-weight: bold;
      color: #ffffff;
      text-shadow: 1px 1px 6px #000;
      z-index: 2;
    }
  </style>
</head>
<body>

  <!-- Video background -->
  <video class="bg-video" autoplay muted loop playsinline>
    <source src="https://tmpfiles.org/dl/8859222/1754223477257.mp4" type="video/mp4">
    Browser Anda tidak mendukung video.
  </video>

  <!-- Musik autoplay (ubah link mp3 jika perlu) -->
  <audio autoplay loop>
    <source src="seeparohaku.mp3" type="audio/mpeg">
  </audio>

  <div class="container">
    <h1>Sahabat yang Asing</h1>
    <p>
      Kita dulu seperti langit dan bintang—selalu berdampingan.<br>
      Tapi waktu membuat kita lupa arah pulang.<br>
      Kini kita hanya dua orang asing yang pernah akrab dalam kenangan.
    </p>

    <div class="buttons">
      <button onclick="showStory()">📝 Baca Cerita</button>
      <button onclick="showMemories()">💭 Kenangan Kita</button>
    </div>

    <div id="story">
      <p>
        Aku masih menyimpan tawa kita di senja yang basah.<br>
        Tapi kini, bahkan suaramu terasa seperti gema dari masa lalu.
      </p>
    </div>

    <div id="memories">
      <p>
        🕊️ 2023 - Kita saling berjanji tak akan berpisah.<br>
        💔 2024 - Aku melihatmu dari kejauhan, tapi kamu menoleh ke arah lain.<br>
        🌌 2025 - Aku masih ingat kamu, meski kamu tak lagi mengenal aku.
      </p>
    </div>
  </div>

  <div class="credit-big">Create by Azka {Noone}</div>

  <script>
    function showStory() {
      document.getElementById('story').style.display = 'block';
    }

    function showMemories() {
      document.getElementById('memories').style.display = 'block';
    }

    document.addEventListener('click', function () {
      document.querySelector('video').muted = false;
    });
  </script>
</body>
</html>
